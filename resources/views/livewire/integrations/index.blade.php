<div class="mx-auto flex w-full max-w-6xl flex-1 flex-col gap-7"><div class="flex items-start gap-4"><span class="app-page-icon hidden shrink-0 sm:inline-flex"><flux:icon name="link" class="size-6" /></span><div><div class="app-eyebrow">Optional connections</div><flux:heading size="xl" class="mt-2 text-3xl tracking-tight">Provider connections</flux:heading><flux:text class="mt-2 leading-6">Keep optional bank and payment connections separate from your records. Secrets are encrypted and never sent to the browser after saving.</flux:text></div></div>@if (session('integration-connected'))<div class="rounded-lg bg-emerald-50 px-4 py-3 text-sm text-emerald-800 dark:bg-emerald-950/30 dark:text-emerald-200">{{ session('integration-connected') }}</div>@endif<div class="grid gap-7 lg:grid-cols-[minmax(0,1fr)_22rem]"><section class="rounded-xl border border-zinc-200 bg-white dark:border-zinc-700 dark:bg-zinc-900"><div class="border-b border-zinc-200 px-5 py-4 dark:border-zinc-700"><flux:heading size="lg">Saved connections</flux:heading></div><div class="divide-y divide-zinc-200 dark:divide-zinc-700">@forelse ($integrations as $integration)@php($integrationTone = match ($integration->status) { 'active', 'connected' => 'success', 'pending', 'connecting' => 'warning', 'failed', 'disconnected' => 'danger', default => 'neutral' })<div class="flex items-center justify-between gap-4 px-5 py-4"><div><div class="font-medium">{{ ucfirst($integration->provider) }} · {{ ucfirst($integration->type) }}</div><div class="mt-2 flex flex-wrap items-center gap-2"><x-status-badge :tone="$integrationTone" :label="ucfirst(str_replace('_', ' ', $integration->status))" /><span class="text-sm text-zinc-500">{{ $integration->metadata['sandbox'] ?? false ? 'Sandbox' : 'Server-configured provider' }}</span></div></div><span class="text-xs text-zinc-500">{{ $integration->last_synced_at?->format('d M Y') ?? 'Not synced' }}</span></div>@empty<div class="px-5 py-10 text-center text-sm text-zinc-500">No provider connections yet.</div>@endforelse</div></section><section class="rounded-xl border border-zinc-200 bg-white dark:border-zinc-700 dark:bg-zinc-900"><div class="border-b border-zinc-200 px-5 py-4 dark:border-zinc-700"><flux:heading size="lg">Add connection</flux:heading></div><form wire:submit="connect" class="space-y-5 px-5 py-5"><flux:select wire:model.live="profileId" label="Profile">@foreach ($profiles as $profile)<flux:select.option value="{{ $profile->id }}">{{ $profile->name }}</flux:select.option>@endforeach</flux:select><flux:select wire:model="provider" label="Provider"><flux:select.option value="sandbox">Sandbox test</flux:select.option><flux:select.option value="stripe">Stripe</flux:select.option><flux:select.option value="paypal">PayPal</flux:select.option><flux:select.option value="wise">Wise</flux:select.option><flux:select.option value="paynet">PayNet / bank</flux:select.option></flux:select><flux:select wire:model="type" label="Connection type"><flux:select.option value="payment">Payment provider</flux:select.option><flux:select.option value="bank">Bank data provider</flux:select.option></flux:select><flux:input wire:model="secret" type="password" label="Provider secret (optional)" autocomplete="off" /><flux:button type="submit" variant="primary" class="w-full">Save securely</flux:button></form></section></div><div class="rounded-lg border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-900 dark:border-amber-950/60 dark:bg-amber-950/30 dark:text-amber-200">Only the local sandbox provider can execute payments in this build. Other providers are stored as encrypted configuration records until their server-side adapter, consent, reconciliation, and webhook rules are configured.</div></div>
