<section class="mt-7 rounded-xl border border-zinc-200 bg-white dark:border-zinc-700 dark:bg-zinc-900">
    <div class="border-b border-zinc-200 px-5 py-4 dark:border-zinc-700"><flux:heading size="lg">Currency conversion</flux:heading><flux:text class="mt-1 text-sm">Add a dated rate to compare foreign-currency records in {{ $profile->base_currency }}. Rates are estimates, not live market quotes.</flux:text></div>
    @if (session('fx-rate-saved'))<div class="mx-5 mt-5 rounded-lg bg-emerald-50 px-4 py-3 text-sm text-emerald-800 dark:bg-emerald-950/30 dark:text-emerald-200">{{ session('fx-rate-saved') }}</div>@endif
    <form wire:submit="save" class="space-y-5 px-5 py-5"><div class="grid gap-5 sm:grid-cols-2"><flux:select wire:model="fromCurrency" label="From" required>@foreach (\App\Domain\Money\Currency::options() as $code => $label)<flux:select.option value="{{ $code }}">{{ $code }} — {{ $label }}</flux:select.option>@endforeach</flux:select><flux:select wire:model="toCurrency" label="To" required>@foreach (\App\Domain\Money\Currency::options() as $code => $label)<flux:select.option value="{{ $code }}">{{ $code }} — {{ $label }}</flux:select.option>@endforeach</flux:select></div><flux:input wire:model="rate" type="number" step="0.0000000001" min="0.0000000001" label="1 From equals" required /><flux:input wire:model="effectiveOn" type="date" label="Effective on" required /><flux:button type="submit" variant="primary" class="w-full">Save exchange rate</flux:button></form>
    <div class="border-t border-zinc-200 px-5 py-4 dark:border-zinc-700"><div class="text-sm font-medium">Saved rates</div>@forelse ($rates as $item)<div class="mt-3 flex justify-between gap-3 text-sm"><span>1 {{ $item->from_currency }} = {{ \App\Domain\Money\Decimal::display($item->rate) }} {{ $item->to_currency }}</span><span class="text-zinc-500">{{ $item->effective_on->format('d M Y') }}</span></div>@empty<div class="mt-3 text-sm text-zinc-500">No exchange rates recorded yet.</div>@endforelse</div>
</section>
